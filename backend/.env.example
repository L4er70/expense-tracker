# Example environment variables for the backend
# Copy this file to `.env` and fill in real values. Do NOT commit your `.env` file.

# MongoDB connection string (MongoDB Atlas or local)
MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.example.mongodb.net/expense-tracker?retryWrites=true&w=majority

# Server port (optional, defaults to 5000)
PORT=5000

# SMTP / Email configuration (used by backend/services/emailService.js)
# If you use Brevo (formerly Sendinblue) SMTP relay, example host: smtp-relay.brevo.com
# Note: smtp-relay.sendinblue.com may also work as Brevo was rebranded from Sendinblue.
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password

# Email addresses
EMAIL_FROM=your_email@example.com
EMAIL_TO=recipient@example.com

# Optional: for third-party API keys or features
# SENDINBLUE_API_KEY=your_sendinblue_api_key

# Notes:
# - Never commit real secrets. Use this file only as a template.
# - If secrets are leaked, rotate them immediately.
