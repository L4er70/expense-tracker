# Frontend example env
# Set the API base URL for the React app (used by src/services/api.js)
REACT_APP_API_URL=http://localhost:5000/api

# If you later deploy the frontend, set REACT_APP_API_URL to your production API URL
